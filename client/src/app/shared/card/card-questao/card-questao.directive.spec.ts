import { CardQuestaoDirective } from './card-questao.directive';

describe('CardQuestaoDirective', () => {
  it('should create an instance', () => {
    const directive = new CardQuestaoDirective();
    expect(directive).toBeTruthy();
  });
});
