import { AvancarDirective } from './avancar.directive';

describe('AvancarDirective', () => {
  it('should create an instance', () => {
    const directive = new AvancarDirective();
    expect(directive).toBeTruthy();
  });
});
