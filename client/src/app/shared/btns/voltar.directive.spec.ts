import { VoltarDirective } from './voltar.directive';

describe('VoltarDirective', () => {
  it('should create an instance', () => {
    const directive = new VoltarDirective();
    expect(directive).toBeTruthy();
  });
});
